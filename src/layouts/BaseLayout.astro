---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';
import { ATTRACTION, SITE_NAME, withSiteName, SITE_URL } from '../data/market';

interface Props {
  title: string;
  description: string;
  image?: string;
  noindex?: boolean;
}
const { title, description, image = '/og-card.png', noindex = false } = Astro.props;
// canonical תמיד מוחלט ומפנה ל-HTTPS; אם Astro.site אינו מוגדר בבנייה, נופלים ל-SITE_URL.
const canonical = Astro.site
  ? new URL(Astro.url.pathname, Astro.site).href
  : new URL(Astro.url.pathname, SITE_URL).href;
const ogImage = Astro.site ? new URL(image, Astro.site).href : image;
const pageTitle = withSiteName(title);
const ogImageAlt = `${ATTRACTION.nameHe} (${ATTRACTION.nameEn}) — ${ATTRACTION.cityHe}, ${ATTRACTION.countryHe}`;
---
<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#1d1813" />
    {noindex && <meta name="robots" content="noindex" />}
    {canonical && <link rel="canonical" href={canonical} />}
    <link rel="alternate" hreflang="he" href={canonical} />
    <link rel="alternate" hreflang="x-default" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon-32x32.png" sizes="32x32" type="image/png" />
    <link rel="icon" href="/favicon-16x16.png" sizes="16x16" type="image/png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-title" content={ATTRACTION.shortNameHe} />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="geo.region" content="IL" />
    <meta name="geo.placename" content={`${ATTRACTION.cityHe}, ${ATTRACTION.countryHe}`} />
    <meta name="ICBM" content={`${ATTRACTION.latitude}, ${ATTRACTION.longitude}`} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="he_IL" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:alt" content={ogImageAlt} />
    {canonical && <meta property="og:url" content={canonical} />}
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />
    <meta name="twitter:image:alt" content={ogImageAlt} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <a class="skip-link" href="#content">דילוג לתוכן</a>
    <Header />
    <main id="content"><slot /></main>
    <Footer />
    <script is:inline>
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          navigator.serviceWorker.register('/sw.js').catch(() => {});
        });
      }
    </script>
    <script is:inline>
      (() => {
        const GA_ID = 'G-HXM22WWPKP';
        const KEY = 'mahaneYehudaCookiePreferences';
        let loaded = false;
        function loadAnalytics() {
          if (loaded || document.querySelector('script[data-ga-loader]')) return;
          loaded = true;
          const script = document.createElement('script');
          script.async = true;
          script.src = `https://www.googletagmanager.com/gtag/js?id=${GA_ID}`;
          script.dataset.gaLoader = 'true';
          document.head.appendChild(script);
          window.dataLayer = window.dataLayer || [];
          window.gtag = function(){ window.dataLayer.push(arguments); };
          window.gtag('consent', 'default', {
            analytics_storage: 'granted',
            ad_storage: 'denied',
            ad_user_data: 'denied',
            ad_personalization: 'denied'
          });
          window.gtag('js', new Date());
          window.gtag('config', GA_ID);
        }
        function readPreferences() {
          try { return JSON.parse(localStorage.getItem(KEY) || '{}'); } catch { return {}; }
        }
        if (readPreferences().analytics === true) loadAnalytics();
        window.addEventListener('cookie-preferences-updated', (event) => {
          if (event.detail?.analytics === true) {
            loadAnalytics();
            if (window.gtag) window.gtag('consent', 'update', { analytics_storage: 'granted' });
          } else if (window.gtag) {
            window.gtag('consent', 'update', {
              analytics_storage: 'denied',
              ad_storage: 'denied',
              ad_user_data: 'denied',
              ad_personalization: 'denied'
            });
          }
        });
      })();
    </script>
  </body>
</html>
